import styled from "styled-components";

export const Link = styled.div`
  font-size: 0.8rem;

  a {
    color: var(--blue-800);
    transition: 0.4s;
  }

  a:hover {
    color: var(--blue-300);
  }
`;
